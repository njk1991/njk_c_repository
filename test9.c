// test9.c
// http://www.runoob.com/cprogramming/c-exercise-example9.html

#include <stdio.h>

int main(int argc, char const *argv[])
{
    for (int i = 0; i < 8; ++i)
    {
        for (int j = 0; j < 8; ++j)
        {
            if ((i + j) % 2 == 0) {
                printf("%c%c",('a' - a + 219),('a' - a + 219));
            } else {
                printf("  ");
            }
        }
        printf("\n");
    }
    return 0;
}